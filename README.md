# My Personal Website

Intended to run each page in different docker containers. Each container is a web server built in a different langage.
GO, Python, Ruby, shell, NodeJS, ...

## BUILD & RUN

The project uses docker compose.
To build : sudo docker-compose build
To run : sudo docker-compose run => triggers a build if images are not built